[workspace]
members = ["crates/*"]
resolver = "3"

# 二进制大小优化配置
[profile.release]
# 启用 LTO（链接时优化），可以移除死代码并减少二进制大小
lto = true
# 优化目标改为大小，而不是速度 ("z" 比 "s" 更激进地优化大小)
opt-level = "z"
# 减少并行代码生成单元到1，允许更多优化
codegen-units = 1
# 出现 panic 时直接终止，而不是展开堆栈，可以移除展开相关代码
panic = "abort"
# 从二进制文件中移除符号信息
strip = true
